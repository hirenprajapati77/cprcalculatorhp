const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.overnightSignal.findMany({ where: { instrumentType: 'INDEX', signalDate: '2026-07-27' }, orderBy: [{ signalTime: 'desc' }, { overnightScore: 'desc' }] }).then(res => { console.log(JSON.stringify(res.map(s => ({ sym: s.symbol, sc: s.overnightScore, dir: s.direction, cb: s.qualityBucket, cl: s.classification })), null, 2)); process.exit(0); });
